// "use server"; // обов'язково!

// export async function handleFormSubmit(formData: FormData) {
//   const username = formData.get("username");

//   // Тут можна зробити: запис у базу, API-запит, логування
//   console.log("Форма надіслана! Імʼя:", username);

//   // Наприклад, можна повернути відповідь (або зробити redirect)
// }
